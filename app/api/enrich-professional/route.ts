import {NextRequest,NextResponse} from 'next/server';
export const maxDuration=30;
const FETCHABLE=['doctoralia.com.br','psitto.com.br','guiasaude.app.br','codental.site','dentmap.com.br','instagram.com','facebook.com','linkedin.com','cro-mg.org.br','crmmg.org.br','crp04.org.br','crefito4.gov.br','escavador.com','cnpj.biz'];
const TRUSTED=/doctoralia\.com\.br|psitto\.com\.br|guiasaude\.app\.br|codental\.site|dentmap\.com\.br|cro-mg\.org\.br|crmmg\.org\.br|crp04\.org\.br|crefito4\.gov\.br/i;
const clean=(s:string)=>s.replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&(?:nbsp|amp);/g,' ').replace(/\s+/g,' ').trim();const norm=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const tokens=(s:string)=>norm(s).replace(/\b(dra?|dr)\.?\b/g,'').split(/\s+/).filter(x=>x.length>2);
function registration(t:string){return t.match(/\b(?:CRP|CRM|CRO|CREFITO|CRN|COREN|CRF)(?:[-\s]?(?:MG|SP|RJ|ES))?\s*(?:N[º°]?\s*)?[\d./-]{3,}\b/i)?.[0]||''}function phones(t:string){return[...new Set((t.match(/(?:\+?55\s*)?(?:\(?\d{2}\)?\s*)?(?:9\s*)?\d{4}[-.\s]?\d{4}/g)||[]).filter(x=>{const n=x.replace(/\D/g,'');return n.length>=10&&n.length<=13}))]}function instagram(t:string){return t.match(/(?:instagram\.com\/|@)([a-zA-Z0-9._]{3,30})/i)?.[1]||''}function address(t:string,city:string){const c=city.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');return t.match(new RegExp(`((?:Rua|R\\.|Avenida|Av\\.|Praça|Pça\\.|Alameda|Travessa|Rodovia)\\s+.{3,180}?(?:${c})(?:\\s*[-–,]?\\s*[A-Z]{2})?)`,'i'))?.[1]?.trim()||''}function clinic(t:string,name:string){const n=norm(name);return t.split(/[|•]/).map(x=>x.trim()).find(x=>x.length<120&&/(cl[ií]nica|consult[oó]rio|instituto|hospital|centro)/i.test(x)&&!norm(x).includes(n))||''}function meta(h:string,k:string){const e=k.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');return(h.match(new RegExp(`<meta[^>]+(?:property|name)=["']${e}["'][^>]+content=["']([^"']+)`,'i'))?.[1]||'').replace(/&amp;/g,'&')}
type R={url:string;title:string;snippet:string;engine?:string;query?:string;score?:number};async function duck(q:string):Promise<R[]>{try{const r=await fetch(`https://html.duckduckgo.com/html/?q=${encodeURIComponent(q)}`,{headers:{'User-Agent':'Mozilla/5.0'},cache:'no-store',signal:AbortSignal.timeout(3000)});if(!r.ok)return[];const h=await r.text();return h.split(/class="result\s/).slice(1).map(b=>{let u=b.match(/class="result__a"[^>]*href="([^"]+)/i)?.[1]||'';try{const x=new URL(u.startsWith('//')?'https:'+u:u),d=x.searchParams.get('uddg');if(d)u=decodeURIComponent(d)}catch{}return{url:u,title:clean(b.match(/class="result__a"[^>]*>([\s\S]*?)<\/a>/i)?.[1]||''),snippet:clean(b.match(/class="result__snippet"[^>]*>([\s\S]*?)<\/(?:a|div)>/i)?.[1]||''),engine:'duckduckgo',query:q}}).filter(x=>/^https?:/.test(x.url)).slice(0,10)}catch{return[]}}async function bing(q:string):Promise<R[]>{try{const r=await fetch(`https://www.bing.com/search?format=rss&q=${encodeURIComponent(q)}`,{headers:{'User-Agent':'Mozilla/5.0'},cache:'no-store',signal:AbortSignal.timeout(3000)}),x=await r.text();return(x.match(/<item>[\s\S]*?<\/item>/gi)||[]).map(i=>({url:clean(i.match(/<link>([\s\S]*?)<\/link>/i)?.[1]||''),title:clean(i.match(/<title>([\s\S]*?)<\/title>/i)?.[1]||''),snippet:clean(i.match(/<description>([\s\S]*?)<\/description>/i)?.[1]||''),engine:'bing',query:q})).filter(x=>/^https?:/.test(x.url)).slice(0,10)}catch{return[]}}async function search(q:string){const[a,b]=await Promise.all([duck(q),bing(q)]);return[...new Map([...a,...b].map(x=>[x.url,x])).values()]}
function score(r:R,name:string,specialty:string,city:string,state:string){const t=norm(`${r.title} ${r.snippet} ${r.url}`),nt=tokens(name),st=tokens(specialty);let s=0,h=nt.filter(k=>t.includes(k)).length;if(h===nt.length)s+=6;else if(h>=Math.min(2,nt.length))s+=3;if(city&&t.includes(norm(city)))s+=4;if(state&&t.includes(norm(state)))s++;if(st.some(k=>t.includes(k)))s+=2;if(TRUSTED.test(r.url))s+=2;return s}async function page(url:string){try{const host=new URL(url).hostname.toLowerCase();if(!FETCHABLE.some(d=>host===d||host.endsWith('.'+d)))return null;const r=await fetch(url,{headers:{'User-Agent':'Mozilla/5.0'},cache:'no-store',signal:AbortSignal.timeout(4000)});if(!r.ok)return null;const html=await r.text();return{html,text:clean(html)}}catch{return null}}
export async function POST(req:NextRequest){try{const b=await req.json(),name=String(b.name||'').trim(),city=String(b.city||'').trim(),state=String(b.state||'MG').trim(),specialty=String(b.specialty||'').trim();if(!name)return NextResponse.json({error:'Nome obrigatório'},{status:400});const primary=[`"${name}" "${city}" ${state}`,`"${name}" "${specialty}" "${city}"`,`"${name}" ${city} telefone whatsapp`,`"${name}" ${city} endereço consultório clínica`,`site:instagram.com "${name}" ${city}`,`site:doctoralia.com.br "${name}"`,`site:guiasaude.app.br "${name}"`,`"${name}" CRO CRM CRP CREFITO CRN ${state}`];let batches=await Promise.all(primary.map(search));let all=[...batches.flat().reduce((m,r)=>m.set(r.url,r),new Map<string,R>()).values()];let scored=all.map(r=>({...r,score:score(r,name,specialty,city,state)})).sort((a,b)=>(b.score||0)-(a.score||0));const nt=tokens(name),ct=norm(city);let relevant=scored.filter(r=>{const t=norm(`${r.title} ${r.snippet}`),hits=nt.filter(k=>t.includes(k)).length;return hits>=Math.min(2,nt.length)&&((ct&&t.includes(ct)&&(r.score||0)>=7)||(r.score||0)>=10||TRUSTED.test(r.url)&&(r.score||0)>=7)});
 // Second pass: broader combinations for local professionals with sparse web presence.
 if(!relevant.length){const shortName=nt.slice(0,2).join(' '),fallback=[`"${name}" Piumhi`,`"${name}" MG`,`"${name}" ${specialty}`,`${name} ${specialty} Piumhi`,`${name} profissional Piumhi`,`${name} clínica Piumhi`,`${name} consultório Piumhi`,`${name} telefone Piumhi`,`${name} WhatsApp Piumhi`,`${name} Instagram Piumhi`,`site:instagram.com ${name} Piumhi`,`site:facebook.com ${name} Piumhi`,`site:linkedin.com ${name} Piumhi`,`site:doctoralia.com.br ${name}`,`site:escavador.com ${name} Piumhi`,`site:cnpj.biz ${name} Piumhi`,shortName?`"${shortName}" ${specialty} ${city}`:''].filter(Boolean);const fb=await Promise.all(fallback.map(search));all=[...new Map([...all,...fb.flat()].map(x=>[x.url,x])).values()];scored=all.map(r=>({...r,score:score(r,name,specialty,city,state)})).sort((a,b)=>(b.score||0)-(a.score||0));relevant=scored.filter(r=>{const t=norm(`${r.title} ${r.snippet} ${r.url}`),hits=nt.filter(k=>t.includes(k)).length,cityHit=ct&&t.includes(ct),specHit=tokens(specialty).some(k=>t.includes(k));return hits>=Math.min(2,nt.length)&&((cityHit&&(r.score||0)>=6)||(specHit&&(r.score||0)>=7)||(r.score||0)>=9)});}
 relevant=relevant.slice(0,24);const docs=(await Promise.all(relevant.slice(0,14).map(async result=>({result,page:await page(result.url)})))).filter(x=>x.page) as any[];let reg='',addr='',cl='',inst='';let ps:string[]=[];const photos:any[]=[],locations:any[]=[];for(const r of relevant){const text=`${r.title} ${r.snippet}`;reg=reg||registration(text);addr=addr||address(text,city);cl=cl||clinic(text,name);inst=inst||instagram(text);ps=[...new Set([...ps,...phones(text)])]}for(const d of docs){const text=d.page.text;reg=reg||registration(text);addr=addr||address(text,city);cl=cl||clinic(text,name);inst=inst||instagram(text);ps=[...new Set([...ps,...phones(text)])];const image=meta(d.page.html,'og:image');if(image&&/^https?:/.test(image)){const x={url:image,sourcePage:d.result.url,title:meta(d.page.html,'og:title')||name,identityScore:d.result.score,status:'awaiting_identity_confirmation'};if(!photos.some(y=>y.url===image))photos.push(x);if(/clinica|consultorio|instituto|hospital|endereco|fachada|unidade/i.test(text)&&!locations.some(y=>y.url===image))locations.push({...x,status:'awaiting_location_confirmation'})}}const sources=relevant.map(x=>({url:x.url,title:x.title,engine:x.engine,query:x.query,identityScore:x.score}));return NextResponse.json({registration:reg,address:addr,clinic:cl,phone:ps[0]||'',whatsapp:ps.find(x=>x.replace(/\D/g,'').length>=11)||ps[0]||'',instagram:inst?`@${inst}`:'',sources,photoCandidates:photos.slice(0,6),locationPhotoCandidates:locations.slice(0,6),facts:{phones:ps.slice(0,4),verifiedPages:docs.length,sourceCount:sources.length,discoveryPass:relevant.length?'primary-or-fallback':'none'},diagnostics:{contact:Boolean(ps.length),location:Boolean(addr||cl),instagram:Boolean(inst),photo:Boolean(photos.length),locationPhoto:Boolean(locations.length),sources:sources.length,verifiedPages:docs.length,queries:primary.length,secondPass:true},found:Boolean(reg||addr||cl||ps.length||inst||photos.length)})}catch(e:any){return NextResponse.json({error:e?.message||'Falha ao enriquecer dados'},{status:500})}}
