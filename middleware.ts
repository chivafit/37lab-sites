import {NextRequest,NextResponse} from 'next/server';
export function middleware(req:NextRequest){const host=(req.headers.get('host')||'').split(':')[0].toLowerCase();const url=req.nextUrl.clone();const platformHosts=new Set(['37lab-sites.vercel.app','localhost','127.0.0.1']);if(platformHosts.has(host)||host.endsWith('.vercel.app')||url.pathname.startsWith('/admin')||url.pathname.startsWith('/demo')||url.pathname.startsWith('/_next')||url.pathname.includes('.'))return NextResponse.next();url.pathname=`/domain/${encodeURIComponent(host)}${url.pathname==='/'?'':url.pathname}`;return NextResponse.rewrite(url)}export const config={matcher:['/((?!api).*)']};